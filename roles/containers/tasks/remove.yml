# Remove Containeirs
# --
- name: Remove Containers
  community.docker.docker_container:
    name: '{{ item }}'
    state: absent
  loop:
    - traefik
    - homepage
    - portainer
    - vaultwarden
    - syncthing
    - grocy
    # - homer
    # - endlessh
    # - watchtower
    # - duplicati
